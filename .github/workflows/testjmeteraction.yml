---
name: Test JMeter Action

on:
  push:
    branches:
      - master
      - feature/*
  pull_request:
    branches: [master]

jobs:

  action_build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      
      - name: Run JMeter Action
        uses: ./
        with:
          testFilePath: Scripts/sample_test.jmx
          outputReportsFolder: ./
          args: "--loglevel INFO"
          plugins: "jpgc-casutg"

      - uses: actions/upload-artifact@v1
        with:
          name: jmeter-test-results
          path: ./

 

